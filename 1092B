#include<bits\stdc++.h>
using namespace std;
int main(void)
{int i,temp=0,n;
cin>>n;
int arr[n];
for(i=0;i<n;i++)
cin>>arr[i];
sort(arr,arr+n);
for(i=0;i<n;i+=2)
temp=temp+(arr[i+1]-arr[i]);
cout<<temp<<endl;
}
